/* Tupple means that each and every types will be same position in the array otherwise its show wrong */
let employ: [string, number] = ["kulsum", 45];

let user: [string, number, boolean] = ["mskulsum@gmail.com", 123, true];
console.log(user);
